{
  "name": "ai-and-web3",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "agents-ts/*",
    "examples-ts/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -ws dev || true",
    "lint": "echo 'no lint configured'"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.5.4"
  }
}
